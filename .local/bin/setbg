#!/bin/bash

if [ -z "$1" ]; then
	bg="$HOME/.local/share/bg.png"
else
	bg="$1"
fi

command wal -e -n -R -o "$HOME/.config/wal/postrun" #> /dev/null 2>&1
feh  --bg-fill --no-fehbg "$bg"
cp -f "$bg" $HOME/.local/share/bg.png 2> /dev/null
