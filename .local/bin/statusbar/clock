#!/bin/bash

case $BLOCK_BUTTON in
	1) notify-send -t 0 "This Month" "$(cal | sed "s/..7m/<b><span color=\"white\">/;s/..27m/<\/span><\/b>/")" ;;
	6) "$TERMINAL" -e "$EDITOR" "$0" ;;
esac

out=$(date "+%a, %d/%m %I:%M%p")
printf "^t^ $out "
